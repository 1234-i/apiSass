services:
  api:
    env_file:
      - .env
      - .env.real-canary
    volumes:
      - ./generated-manifests:/app/generated-manifests
      - ./mock-runtime:/app/mock-runtime
      - ./real-kubeconfig:/app/.kube:ro
